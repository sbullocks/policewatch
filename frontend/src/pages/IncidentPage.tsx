import { Box, Typography } from '@mui/material';

export default function IncidentPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">Incident Detail</Typography>
      <Typography color="text.secondary">Phase 4 — coming soon.</Typography>
    </Box>
  );
}
