import { Box, Typography } from '@mui/material';

export default function RecordPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">Report an Incident</Typography>
      <Typography color="text.secondary">Phase 2 — coming soon.</Typography>
    </Box>
  );
}
